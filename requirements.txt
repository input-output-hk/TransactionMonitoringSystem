# Core Framework
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2

# WebSocket Client (Ogmios)
websockets>=16.0,<17.0

# Database Drivers
clickhouse-driver==0.2.10
asyncpg==0.31.0

# HTTP client for the clustering sidecar reverse-proxy (app/api/clustering.py).
# Pinned to match the sidecar's resolved httpx so the two stay in lockstep.
httpx==0.28.1

# Email (magic-link auth)
aiosmtplib==3.0.2
email-validator==2.3.0  # Pydantic's EmailStr requires this

# Configuration
python-dotenv==1.2.2
PyYAML==6.0.3

# Analysis Engine (Milestone 2)
cbor2==6.1.2
rapidfuzz==3.14.5
tldextract==5.3.1

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# Development Tools
black==26.5.1
ruff==0.15.18
mypy==2.1.0
