# Production runtime dependencies only. Test and development tooling lives
# in requirements-dev.txt; the backend Docker image installs the fully
# pinned + hashed resolution of this file (requirements.lock).
#
# After changing anything here, regenerate the lock:
#   uv pip compile requirements.txt -o requirements.lock \
#     --universal --generate-hashes --python-version 3.12

# Core Framework
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2

# WebSocket Client (Ogmios)
websockets>=16.0,<17.0

# HTTP client: webhook notification delivery + the clustering sidecar
# reverse-proxy (app/api/clustering.py). The sidecar itself only uses httpx
# in its dev extra (test client), so no cross-repo version coupling remains;
# this pin is just the newest release validated against both call sites.
httpx==0.28.1

# Database Drivers
clickhouse-driver==0.2.10
asyncpg==0.31.0

# Email (magic-link auth)
aiosmtplib==5.1.2
email-validator==2.3.0  # Pydantic's EmailStr requires this

# Configuration
python-dotenv==1.2.2
PyYAML==6.0.3

# Analysis Engine (Milestone 2)
cbor2==6.1.2
rapidfuzz==3.14.5
tldextract==5.3.1
