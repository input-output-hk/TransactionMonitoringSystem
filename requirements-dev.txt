# Test and development tooling. Never installed in the production image
# (backend/Dockerfile installs requirements.lock, the hashed resolution of
# requirements.txt only).
-r requirements.txt

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# Development Tools
black==26.5.1
ruff==0.15.18
mypy==2.1.0
