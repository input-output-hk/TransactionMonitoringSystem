"""Notification delivery channels.

Each channel implements the :class:`~app.notifications.channels.base.NotificationChannel`
ABC. The set of live channels is assembled by ``app.notifications.registry``.
"""
