/**
 * Links to the public user documentation (the input-output-hk/
 * TransactionMonitoringSystem repo). Kept in one place so the advisory callouts
 * and any future help links can't drift apart. Anchors match the headings in
 * docs/CLUSTERING.md (GitHub slugifies them lowercase, spaces → hyphens).
 */
const DOCS_BASE =
	"https://github.com/input-output-hk/TransactionMonitoringSystem/blob/main/docs";

export const DOCS_CLUSTERING = `${DOCS_BASE}/CLUSTERING.md#clustering`;
export const DOCS_ANOMALY = `${DOCS_BASE}/CLUSTERING.md#anomaly-detection`;
